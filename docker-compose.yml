services:
  web:
    build:
      context: .
      args:
        VITE_GA_MEASUREMENT_ID: ${VITE_GA_MEASUREMENT_ID:-}
        VITE_FX_RATES_API_URL: ${VITE_FX_RATES_API_URL:-}
    ports:
      - "3000:3000"
